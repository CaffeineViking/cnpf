#include "Environment.hpp"
#include <iostream>
void Environment::addLight(const Light& light){
/*
	if(std::find(_lights.begin(), _light.end(), light) == _lights.end()){
		std::cerr << "Tried to add light already in environemnt" << std::endl;
		return;
	}
	//_lights.push_back(light);
	*/
}

void Environment::removeLight(const Light& light){
	/*
	try{
	//	_lights.remove(light);
	}catch(...){
		std::cerr << "Tried to remove light not in environemnt!" << std::endl;
	}
	*/
}

void Environment::begin(const GLuint& programId){

}

void Environment::end(){

}